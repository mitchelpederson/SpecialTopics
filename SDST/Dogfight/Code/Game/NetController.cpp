#include "Game/NetController.hpp"


//----------------------------------------------------------------------------------------------------------------
NetController::NetController() {

}


//----------------------------------------------------------------------------------------------------------------
void NetController::Update() {

	/*throttle = entity->currentState.throttle;
	rollAxis = entity->currentState.rollAxis;
	yawAxis = entity->currentState.yawAxis;
	pitchAxis = entity->currentState.pitchAxis;
	isFireMissilePressed = entity->currentState.isFireMissilePressed;
	isFireGunPressed = entity->currentState.isFireGunPressed;*/

}